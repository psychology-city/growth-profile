# 任务：修复完整测评不显示子维度详情

## 问题
完整测评（432题）完成后的结果页，只显示五大维度的分析，不显示27个子维度的详细得分和建议。

原因：代码中用 `if (this.state.selectedDimension !== 'full')` 限制了子维度卡片的显示。

## 修复内容

### app.js（第606-643行）
- 删除了 `!== 'full'` 条件判断
- 新逻辑：无论单维度还是完整测评，都显示子维度详情
- 完整测评时：遍历全部五大维度，按维度分组显示27个子维度
- 单维度测评时：只显示该维度的子维度

关键代码变更：
```javascript
// 之前：仅单维度测评
if (this.state.selectedDimension !== 'full' && this.state.selectedDimension) {
    const dim = AssessmentData.dimensions[this.state.selectedDimension];
    // 显示 dim.subcategories...
}

// 之后：所有测评都显示
const targetDimensions = this.state.selectedDimension && this.state.selectedDimension !== 'full'
    ? [AssessmentData.dimensions[this.state.selectedDimension]].filter(Boolean)
    : Object.values(AssessmentData.dimensions);  // 全部五大维度

targetDimensions.forEach(dim => {
    analysisHtml += `<div class="dimension-group-header">${dim.icon} ${dim.name}</div>`;
    dim.subcategories.forEach(subId => {
        // 显示每个子维度卡片...
    });
});
```

### styles.css（第702行起新增）
- `.dimension-group-header` 样式：五大维度的分组标题，带左侧边框和背景色

## 修复后完整测评结果页文字内容
1. 日期
2. 五维雷达图（ECharts）
3. 5张维度得分卡片（每张：名称+分数+等级）
4. 五大维度详细分析（每维度：title + summary + analysis + 3条advice）
5. **📋 子维度详情（新增）**
   - 🧠 认知能力（分组标题）
     - 注意力卡片（分数+等级+title+content+practice）
     - 观察力、记忆力、思维力...（共9个）
   - ❤️ 人际共情（分组标题）
     - 理解他人、社会感知...（共4个）
   - 💪 意志行动（分组标题）
     - 自我控制、目标执行...（共4个）
   - 👔 领导职业（分组标题）
     - 领导力、团队协作...（共4个）
   - 🌟 超越动力（分组标题）
     - 意义追寻、自我超越...（共6个）
6. 分享按钮

## 时间
2026-04-17 22:07 GMT+8
